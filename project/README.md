# Index Sample Project

A minimal Node.js sample project created inside the `index` folder.

## Usage

1. Open a terminal in `index/project`
2. Run `npm install` (optional, no dependencies)
3. Run `npm start`
